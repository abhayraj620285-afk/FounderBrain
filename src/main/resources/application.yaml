spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/founderbrain
    username: abhayraj
    password:

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  security:
    user:
      name: admin
      password: admin

server:
  port: 8080
